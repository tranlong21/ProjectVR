UpdateBlog

Hệ thống viết blog hoạt động theo mô hình Visual ↔ HTML giống WordPress, có tích hợp AI để tạo bản nháp nội dung.

1) Luồng tạo nội dung
- Người dùng cung cấp dữ liệu thô: ý tưởng, đoạn text rời rạc, hình ảnh, link tham khảo.
- AI phân tích dữ liệu thô và sinh ra nội dung có cấu trúc ngữ nghĩa (semantic structure) như: tiêu đề, heading, đoạn văn, hình ảnh, liên kết.
- AI KHÔNG sinh HTML hiển thị trực tiếp cho người dùng.

2) Visual Editor (tab soạn thảo)
- Người dùng chỉ nhìn thấy chữ và bố cục giống bài blog thật:
  + Tiêu đề to / nhỏ (H1, H2…)
  + Đoạn văn
  + Ảnh
  + Link
- Người dùng KHÔNG thấy thẻ HTML.
- Có thể chỉnh sửa nội dung, màu sắc, kích thước chữ, căn chỉnh, thứ tự các phần.
- Visual Editor render nội dung từ dữ liệu đã parse, không phụ thuộc AI.

3) HTML Editor (tab HTML)
- Hiển thị mã HTML tương ứng với nội dung đang soạn.
- Dành cho người dùng nâng cao / SEO / kỹ thuật.
- Khi chỉnh HTML, hệ thống parse ngược lại để đồng bộ Visual Editor.
- Visual ↔ HTML luôn đồng bộ 2 chiều.

4) Nguyên tắc sinh HTML
- HTML KHÔNG do AI sinh.
- HTML được sinh bằng code (JS / backend) từ cấu trúc nội dung đã kiểm soát.
- Đảm bảo HTML sạch, đúng semantic, chuẩn SEO.

5) Lưu dữ liệu trong MySQL (CHỐT)

Bảng blog_posts:
- id (BIGINT, PK)
- title (VARCHAR)
- slug (VARCHAR, UNIQUE)
- content_html (LONGTEXT)  ← HTML cuối cùng sau khi render
- content_source (LONGTEXT) ← nội dung nguồn do AI sinh (semantic text / HTML-lite)
- thumbnail_url (VARCHAR)
- status (draft / published)
- created_at
- updated_at

Nguyên tắc lưu:
- content_source:
  + Lưu nội dung có cấu trúc ngữ nghĩa (dùng cho editor, AI, chỉnh sửa tiếp).
- content_html:
  + Lưu HTML hoàn chỉnh để hiển thị frontend.
- KHÔNG lưu nhiều phiên bản HTML khác nhau.
- content_html là dữ liệu dùng để render blog ngoài public.
- content_source là dữ liệu dùng để mở lại editor và chỉnh sửa.

6) Vai trò AI
- AI chỉ tạo bản nháp nội dung ban đầu (content_source).
- AI không tự publish.
- AI không sao chép nguyên văn từ website khác.
- Người dùng là người quyết định nội dung cuối cùng.

👉 Tóm lại:
AI tạo bản nháp có cấu trúc → Editor cho người dùng chỉnh → Hệ thống sinh HTML → Lưu MySQL → Hiển thị blog.
- Giao diện có thể là giống hệt Wordpress nhưng thêm phần AI sẽ sinh nội dung nháp
